@inherits LayoutComponentBase
@inject AppState State

<div class="app-layout">
    @if (State.IsFirstRun)
    {
        <SetupWizard OnComplete="() => { State.IsFirstRun = false; StateHasChanged(); }" />
    }
    else
    {
        <AttacheGui.Components.Pages.Dashboard />
    }
</div>
